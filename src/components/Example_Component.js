import React, {  } from 'react'; //useContext
// import { AppContext } from '../context/AppContext';
const Example_component = () => {

    return (
        <div>
            <span>This is where i left off for tonight!</span>
        </div>
    );
};

export default Example_component;